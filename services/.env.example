# Optional service-local copy for the predictive dialer.
# You can also keep the shared config in the project root `.env` file.

DB_HOST=localhost
DB_USER=your_db_user
DB_PASS=your_db_password
DB_NAME=dialerwave
# Use the subfolder URL or the direct subdomain URL, whichever is the real public app URL.
APP_BASE_URL=https://your-domain.com/newwave
